export enum Estado {
  ACTIVO = 'activo',
  INACTIVO = 'inactivo',
  COMPLETADO = 'completado',
}

export enum Role {
  USUARIO = 'usuario',
  ADMIN = 'admin',
}

export enum TipoName {
  DESPACHO = 'despacho',
  PRODUCCION = 'produccion',
}
