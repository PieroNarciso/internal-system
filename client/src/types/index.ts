export enum Role {
  ADMIN = 'admin',
  USUARIO = 'usuario',
};

export enum Estado {
  ACTIVO = 'activo',
  INACTIVO = 'inactivo',
  COMPLETADO = 'completado',
};

export enum TipoNombre {
  DESPACHO = 'despacho',
  PRODUCCION = 'produccion',
};
