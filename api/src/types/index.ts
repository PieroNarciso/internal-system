export enum Estado {
  ACTIVO = 'activo',
  INACTIVO = 'inactivo',
  COMPLETADO = 'completado',
}

export enum TipoNombre {
  DESPACHO = 'despacho',
  PRODUCCION = 'produccion',
}

export enum Rol {
  ADMIN = 'admin',
  USUARIO = 'usuario',
}
