export enum Estado {
  ACTIVO = 'activo',
  INACTIVO = 'inactivo',
  COMPLETADO = 'completado',
}

export enum TipoNombre {
  DESPACHO = 'despacho',
  PRODUCCION = 'produccion',
}

export enum Role {
  ADMIN = 'admin',
  USUARIO = 'usuario',
}
